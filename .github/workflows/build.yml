name: Build
on: push

jobs:
  compile:
    runs-on: ubuntu-latest
    name: Compile Firmware
    steps:
    - name: Hello world action step
      id: hello
      uses: interactionresearchstudio/YoYoMachines-FirmwareCompiler
      with:
        repo-name: 'ESP32-SOCKETIO'
